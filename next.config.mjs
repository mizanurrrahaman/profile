/** @type {import('next').NextConfig} */
const nextConfig = {
   images: {
     domains: ['magesolution.com', 'repository-images.githubusercontent.com', 'img.freepik.com'],
    },
};

export default nextConfig;
